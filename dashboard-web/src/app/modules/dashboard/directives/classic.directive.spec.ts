import { ClassicDirective } from './classic.directive';

describe('ClassicDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassicDirective();
    expect(directive).toBeTruthy();
  });
});
